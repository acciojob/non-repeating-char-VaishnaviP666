//your JS code here. If required.
let index = -1;
let fnc = ' ';
hasKey(key)
{
for (let i of string) {
    if (string.split(i).length - 1 === 1) {
        fnc = i;
        break;
    } else {
        index += 1;
    }
	return fnc;
}
}
const key = prompt("Enter a string");
alert(hasKey(key));